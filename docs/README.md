# Documentation index

Start with `../README.md`, `ENGINE_STATE_BOUNDARY.md`, `ARCHITECTURE.md`, `OWNER_CONSOLE.md` and `SELF_MAINTENANCE_BOOTSTRAP.md`.

`../PROTOCOL.md` and `../protocol/v2/` are the wire/transition authority. `../SUPERVISOR_ENTRYPOINT.md` and `../policies/` are generic operational policy. Generic operator safety and containment architecture documents are retained as implementation references, not proof of host installation. Production reports, machine paths, historical acceptance output and concrete instance snapshots do not belong in this public repository.
